import { Box } from "@mui/material";
import React from "react";
import Header from "../components/Header";

const Main: React.FC = () => {
  return (
    <Box>
      <Header />
    </Box>
  );
};

export default Main;
